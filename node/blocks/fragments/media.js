const mediaFields = `
    altText
    mediaItemUrl
    xs: sourceUrl(size: THUMBNAIL)
    md: sourceUrl(size: MEDIUM)
    mlg: sourceUrl(size: MEDIUM_LARGE)
    lg: sourceUrl(size: LARGE)
    uri
`

module.exports = mediaFields